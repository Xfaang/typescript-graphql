import { int } from '../../types';

/** adds one number to another */
export function sum(a: int, b: int): int {
  return a + b;
}

/** subtracts one number from another */
export function diff(a: int, b: int): int {
  return a - b;
}
